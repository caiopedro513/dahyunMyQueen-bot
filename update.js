let update = function(target, client){
    client.say(target, `slots are harder now having 25 emotes, if you do $slot and dont provide any value it will be set to $10, minimum value for slots are $10 now`);
}

module.exports = {update}